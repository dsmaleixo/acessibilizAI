export default function MapaPage() {
  // TODO(US-17..US-21): mapa (Leaflet) com pontos acessíveis + barreiras abertas,
  // roteamento por perfil e SEMPRE uma alternativa em lista/texto (acessibilidade).
  return (
    <section className="mx-auto max-w-3xl p-6">
      <h1 className="text-2xl font-bold">Mapa de rotas acessíveis</h1>
      <p className="mt-2">Mapa e roteamento do piloto (a implementar).</p>
    </section>
  );
}
